# 1. template.py 
# 2. excute python template.py

set our src folder to package so that we need to add some condition 
    setup.py ( this code set our folder python files as package)

    -e . this will help to this will open setup.py set all the __init__.py as package

1. Load data in helper.py (Load method)

2 . store_index (reset you pinecone index)

3.  Implement the pinecone connection in store_index.py files 
        it will take some time because it load all files and split chunks and and perform embeddings then store to PineconeDB
        Fine excuted
